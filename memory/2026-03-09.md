2026-03-09

- Implemented SUB-7 session intervention flow in `packages/ui`.
- Added persisted intervention history in Zustand via `localStorage`.
- Intervention entries now store transport/error metadata and update in real time.
- Chat input now supports `Ctrl+Enter` / `Cmd+Enter` for interventions and shows clearer helper text.
- Installed workspace dependencies with `npm install` to run validation.
